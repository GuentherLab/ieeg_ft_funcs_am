For LLM (add a var to the epochs table specifying the start and end times of that epoch relative to trial start): 
-i want you to write code for doing timewarping of neural responses timecourses; each trial of the experiment has a fixed set of events that always occur in the same order but may have some variable durations between them; i want you to linearly timewarp the neural response timecourses for each trial, so that there is always a fixed amount of neural response timepoints in each epoch
-the overall structure of my script is: response_types_daf is upper level, and calls get_epoched_responses. The intention is for your new code to be inside get_epoched_responses, so that after that script chops up the non-warped neural response timecourses for each trial (in ‘resp.timecourse_unwarped’), you then add a variable resp.timecourse which is structured similarly, except that it has identical numbers of timepoints for a given epoch across all trials
-I’ve included a screenshot of how the ‘trials_beh’ table looks, which will need to be used to figure out the events defining each epoch on each trial
-the specific epochs which should have the same durations on each trial are specified by a table op.epochs - see screenshot. For each epoch duration, use the value in ‘dur_fix’ 
---epochs are defined in one of two ways:
------ just a string, specified the name of the event in the trials table where it should start/stop
----- a string plus a number - this is the number is seconds which this epoch should be shifted from the event; e.g. the onset of ‘base’ epoch is defined as ‘t_vis_stim_on’,-0.5…. Meaning it starts 0.5sec before t_vvis_stim_on
-all epochs within the epochs table should be treated the same, except for ‘trial’. The ‘trial’ epoch must always start at time zero and should never end before any of the other epochs. It defines the duration of all of timecourse. There will be some parts of the ‘trial’ epoch that are not covered by other epochs, e.g. times before the first other epoch  and times after the final other epoch (and possibly intervening ones) - do time warping on these ‘filler’ epochs the same as with other epochs, such that e.g. there is always the same amount of time after trial start and before basline onset, even if this specific epoch is not named in the epoch table
-in some cases warping wouldn’t technically be necessary; e.g. ‘base’ epoch is defined as -0.5s to -0.1s relative to t_vis_stim_on, so this will already always be 0.4s duration
-do a check for each epoch aside from ‘trial’ that they do not overlap
….
we need to build in one exception to the no-epoch-overlaps rule: sometimes the subject answered early; so I’ve added a column to the epochs table called ‘early_overlap_allowed’; for any epoch that this is true for, do not throw an error if it overlap with the preceding segment; instead, the epoch where this option is enabled takes precedence, and the overlapping timepoints go to this segment; but throw a warning for trials in which this occurs. Also, add a column to the trials table with the name ‘X_epoch_early’, where X is the epoch name, and the value here is the amount of time that it stole from preceding segments (give a value of 0 on trials where it did not); the purpose of this is to allow for including trials where the subject answered before the GO beep, while still giving a warning about it and recording how early they responded; thoroughly comment this part
….
Next addition: in the trials table, I want ‘times_unwarped’ to store the timepoints within the whole trial epoch before warping. Then i want the addition of a ‘times’ column (ignore what we previously were putting in the 'times' column); 'times' should hold the timepoints after warping; these times will no longer have a fixed sample rate and will no longer map 1:1 to the values of D_in.time, because they will represent the stretching/shrinking that you did for each of the epochs 
….
let’s change the approach. I want to get rid of the ‘trial’ epoch, because I don’t want there to be any unlabeled gaps between the labeled epochs. So: as a key additional check, check for each trial that there are no gaps between the epochs (greater than the average unwarped timestep duration)
….
Add another check: the epochs as listed in the epochs table should always be found within a trial in the same order they are specified in the epochs table
