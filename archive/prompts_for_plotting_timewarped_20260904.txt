
I have a set of matlab functions which plot timecourses of electrodes - averaged across trials for a single electrode, and then also across electrodes at the level of combine_plot_electrode_timecourses. The plotting function also displays the average time of key events occurring within each trial (stim onset, speech onset/offset etc). My main challenge: these plotting functions were created under the assumption that the electrode timecourses within resp.timecourses would be unaligned - meaning that the time between e.g. stim offset and onset, within key epochs, would vary across trials, so that the scripts would need to be given a sync event, which would be treated as time zero for all trials. However, my response values in resp.timecourses have now been warped and pre-aligned, so that that key events all occur at the same time to each other and there are identical numbers of timepoints with identical spacing within each trial. (the ‘global time coordinates’ of these timepoints for each trial are stored in trials.times, and have identical GTC spacing in every trial). First let’s strategize about ways to modify - and significantly simplify - these functions, under the assumption that warping has been done. In the workspace there will generally be a table epochs, with column dur_fix, specifying how much time each of the epochs is supposed to last. These epochs are expected to be in order, and contiguous, so that when plotting a trial, labels could be applied to x values stating when epochs are occurring, and all epochs have a label, with no gaps
……
- something else I’d like added is to include an option in the op structure called ‘op.epochs_to_label’... if this empty, then the timecourse plot should have a text annotation box for all events; these annotation boxes will be like what we  had in the old version of the script, except that in the older version, they were aligned with events (e.g. speech onset), whereas now they should be centered with epochs (e.g. stim epoch, speech epoch); if op.epochs_to_lable is not empty, then it should be a cell array of strings, matching the names of some of the epochs in the epochs table; throw an error if this is formatted wrong or if it lists an event not in the epochs table; the epochs listed in this option should be those for which a text annotation is displayed above the timecourse
-plot_resp_timecourse - create as you described
-combine_plot_electrode_timecourses_seq.m - create as you described… the idea is that this is a top-level script, which only contains parameters specified for a particular project (in this case the ‘seq’ project)... other projects will use the same basic functionality/formatting of having timewarped electrode responses and epochs to load, but those epochs may be specified differently, as well as different trial conditions and tuning parameters for electrodes…. And the functions which get called by scripts like this will be generic and project-agnostic… the top level script will be a place to specify op.epochs_to_label and the other parameters already specified there like op.sort_cond and op.tuning_param
-then, try to combine into a single function the functionality of (to the extent that we still need all of these) combine_plot_electrode_timecourses.m, align_timecourses, sort_responses_by_condition.m, and the other subfunctions you described. This big function will be called by combine_plot_electrode_timecourses_seq.m, and should be name (as before, though it’s doing a lot more now) combine_plot_electrode_timecourses.m
.....
in plot_resp_timecourse.m
-add epoch dividers - xlines showing the boundary of the epochs, dividing up the colored shaders.
---add options 'epoch_divider_linestyle' (default '.'),'epoch_divider_linewidth'(default 3) - format these use with op field_default